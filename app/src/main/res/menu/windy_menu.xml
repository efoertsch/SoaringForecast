<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/windy_menu_select_task"
        android:title="@string/forecast_menu_select_task"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/windy_menu_clear_task"
        android:title="@string/forecast_menu_clear_task"
        app:showAsAction="never"/>

    <item
        android:id="@+id/windy_menu_topo_map"
        android:title="@string/windy_menu_topo_map"
        android:checkable="true"
        app:showAsAction="never"/>

</menu>